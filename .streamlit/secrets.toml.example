# Streamlit Cloud / HF Space secrets — copy to secrets.toml or paste into
# the deploy host's "Secrets" UI. secrets.toml is git-ignored.

GROQ_API_KEY = "gsk_your_key_here_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Or, if you want OpenAI as a default:
# OPENAI_API_KEY = "sk-your-key-here"
